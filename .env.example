# Supabase Configuration (Required)
# Get these values from your Supabase project dashboard
# Project Settings > API

VITE_SUPABASE_URL=https://your-project-id.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# Google Gemini API (Optional - for AI chat features)
# Get your API key from: https://aistudio.google.com/app/apikey
# If not provided, the app will use fallback chatbot logic
VITE_GOOGLE_API_KEY=your-google-api-key-here

# Instructions:
# 1. Copy this file to .env in the same directory
# 2. Replace the placeholder values with your actual credentials
# 3. Never commit .env file to git (it's in .gitignore)
#
# For Vercel deployment:
# - Add GOOGLE_API_KEY (without VITE_ prefix) in Vercel dashboard for /api functions
# - Add VITE_GOOGLE_API_KEY (with VITE_ prefix) for client-side direct API calls
# - Add VITE_SUPABASE_URL and VITE_SUPABASE_ANON_KEY in Vercel dashboard
#
# Note: VITE_ prefix is required for Vite to expose env vars to the browser

